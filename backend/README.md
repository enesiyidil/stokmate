# StokMate backend

Spring Boot API for StokMate. Full project documentation lives in the [repository root README](../README.md).

```bash
cp .env.example .env
docker compose up -d
mvn spring-boot:run
```

Swagger UI: http://localhost:9090/swagger-ui/index.html
