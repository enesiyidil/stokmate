# stokmate-ui has moved

This repository is archived.

The UI now lives with the API in a single monorepo:

**https://github.com/enesiyidil/stokmate**

Clone that repository and follow the root README (`docker compose up --build`).
