# StokMate frontend

React + Vite UI for StokMate. Full project documentation lives in the [repository root README](../README.md).

```bash
cp .env.development.example .env.development
npm install
npm run dev
```

Dev server: http://localhost:5173 (proxies `/api` to `http://localhost:9090`).
